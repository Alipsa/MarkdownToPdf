<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <!--
  Standalone launcher POM — runs the MarkdownToPdf GUI via the javafx-maven-plugin:

    mvn -f MarkdownToPdf.xml javafx:run

  This is an alternative to the fat-jar distribution that does not require a
  JavaFX-bundled JDK; the javafx-maven-plugin downloads and wires JavaFX automatically.

  Prerequisites:
  - JDK 25 or higher
  - Maven 3.9.9 or higher
  - se.alipsa:MarkdownToPdf:0.1.1-SNAPSHOT installed in the local Maven repository
    (run "mvn install" from the project root first)
  -->
  <modelVersion>4.0.0</modelVersion>
  <groupId>se.alipsa</groupId>
  <artifactId>MarkdownToPdf-launcher</artifactId>
  <version>0.1.1-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>MarkdownToPdf Launcher</name>

  <properties>
    <javafx.version>25.0.4</javafx.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>se.alipsa</groupId>
      <artifactId>MarkdownToPdf</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-swing</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-web</artifactId>
      <version>${javafx.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.6.3</version>
        <executions>
          <execution>
            <id>enforce-releases-maven-java</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[3.9.9,)</version>
                  <level>ERROR</level>
                  <message>Please use maven version 3.9.9 or higher</message>
                </requireMavenVersion>
                <requireJavaVersion>
                  <version>[25,)</version>
                  <level>ERROR</level>
                  <message>Please use Java 25 or higher</message>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
        <executions>
          <execution>
            <id>default-compile</id>
            <phase>compile</phase>
            <goals>
              <goal>compile</goal>
            </goals>
            <configuration>
              <skipMain>true</skipMain>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>se.alipsa.md2pdf.gui.MarkdownToPdf</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
