<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <!--
  Launcher POM — runs the MarkdownToPdf GUI via the javafx-maven-plugin:

    mvn -f MarkdownToPdf.xml javafx:run

  This is an alternative to the fat-jar distribution that does not require a
  JavaFX-bundled JDK; the javafx-maven-plugin downloads and wires JavaFX automatically.
  It inherits ../pom.xml as its parent, so it is not copyable out of the repo as a
  standalone file — it must stay alongside the rest of the project.

  Prerequisites:
  - JDK 25 or higher
  - Maven 3.9.9 or higher
  - the matching se.alipsa:MarkdownToPdf version installed in the local Maven repository
    (run "mvn install" from the project root first)
  -->
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>se.alipsa</groupId>
    <artifactId>md2pdf-parent</artifactId>
    <version>${revision}</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <artifactId>MarkdownToPdf-launcher</artifactId>
  <packaging>pom</packaging>
  <name>MarkdownToPdf Launcher</name>

  <dependencies>
    <dependency>
      <groupId>se.alipsa</groupId>
      <artifactId>MarkdownToPdf</artifactId>
      <!-- gui's own version — must match gui/pom.xml's <version> exactly. Nothing else
           builds, tests, or touches this file (only CLAUDE.md references it), so a stale
           value here is invisible until someone runs `mvn -f gui/MarkdownToPdf.xml
           javafx:run`. release.sh's gui flow checks this stays in sync. -->
      <version>0.2.0</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-swing</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-web</artifactId>
      <version>${javafx.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.6.3</version>
        <executions>
          <execution>
            <id>enforce-releases-maven-java</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[3.9.9,)</version>
                  <level>ERROR</level>
                  <message>Please use maven version 3.9.9 or higher</message>
                </requireMavenVersion>
                <requireJavaVersion>
                  <version>[25,)</version>
                  <level>ERROR</level>
                  <message>Please use Java 25 or higher</message>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
        <executions>
          <execution>
            <id>default-compile</id>
            <phase>compile</phase>
            <goals>
              <goal>compile</goal>
            </goals>
            <configuration>
              <skipMain>true</skipMain>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <configuration>
          <!-- This standalone POM shares gui/ with the reactor GUI module. -->
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>se.alipsa.md2pdf.gui.MarkdownToPdf</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
